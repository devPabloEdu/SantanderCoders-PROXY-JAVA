package proxy.problema;

import proxy.Pessoa;
import proxy.PessoaBuilder;
import proxy.PessoaRepository;
import proxy.PessoaService;

public class PessoaTest {
    public static void main(String[] args) {
        /*PessoaService pessoaService = new PessoaService(new PessoaRepository());

        Pessoa pessoa = new PessoaBuilder()
                .sobreNome("piotto")
                .dataDoNascimento("03/06/2021")
                .nome("anderson")
                .apelido("careca")
                .documento("40028922400")
                .build();
        pessoaService.save(pessoa);

        Pessoa pessoaRetornada = pessoaService.findById(1L);
        System.out.println(pessoaRetornada);*/
    }
}
